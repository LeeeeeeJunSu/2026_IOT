# Raspberry Pi 5 / Ubuntu 24.04 deep-model inference requirements.
#
# Install after the common runtime requirements:
#   python -m pip install -r requirements.txt
#   python -m pip install -r requirements-pi-cpu.txt
#
# Use the official PyTorch CPU wheel index. Do not install the default PyPI
# torch wheel on Raspberry Pi if you want a CPU-only deployment; newer PyTorch
# PyPI wheels may include CUDA dependencies on Linux aarch64.

--index-url https://download.pytorch.org/whl/cpu
torch==2.11.0
