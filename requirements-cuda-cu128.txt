# NVIDIA CUDA 12.8 workstation training requirements.
#
# Install after the common runtime requirements:
#   python -m pip install -r requirements.txt
#   python -m pip install -r requirements-cuda-cu128.txt
#
# This is the environment used for RTX 5070 Ti training in this workspace.

--index-url https://download.pytorch.org/whl/cu128
torch==2.11.0
